package com.cougar.maksim.fastnotes.DataClasses

enum class NoteStatus(val stringVal:String) {
    //TODO use resourses for strings (no context)
    ALWAYS("Always"), NEVER("Never"), ALL_BEFORE_DATE("All before date"), AT_DAY("At day")

}